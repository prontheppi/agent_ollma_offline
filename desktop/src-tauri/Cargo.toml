[package]
name = "enterprise-offline-ai"
version = "0.1.0"
description = "Offline enterprise AI document assistant"
authors = ["EnterpriseOfflineAI"]
edition = "2021"

[lib]
name = "enterprise_offline_ai_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
